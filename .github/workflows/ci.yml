name: CI

on:
  push:
  pull_request:

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    container: wpilib/debian-base:trixie

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Build project
        working-directory: example/example_robot_project
        run: ./gradlew assemble

  test:
    name: Test
    runs-on: ubuntu-latest
    container: wpilib/debian-base:trixie

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Run test suite
        working-directory: example/example_robot_project
        run: ./gradlew test
