# Vector3

Core vector utilities for positions, velocities, and forces.

Examples

```py
v = Vector3(1, 0, 0)
v.rotate(axis, theta)
```

See code in `cad-import/geometry_utils.py` for Python helpers.
