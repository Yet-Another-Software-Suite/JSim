# Files & Source Locations — Guide

Where to read implementation
- Core engine: `core/src` and `core/include`
- CAD import and materials: `cad-import/`
- Examples and runtime: `examples/`, `apps/`, and `jsim/examples`

Tip
- Read the example that is closest to your intended use case, then follow symbols into the implementation for details.

