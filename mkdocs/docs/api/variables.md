# API Variables & Constants — Guide

Common uses
- Constants for physical defaults (gravity, standard friction) and build-time version strings are defined in headers and top-level config files.

Finding definitions
- Search `core/include` and top-level configuration files to locate declarations and default values.

