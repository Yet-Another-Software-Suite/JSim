# Namespace Members — Guide

What they are
- Namespace members include free functions, enums, typedefs, and constants used across modules. They are grouped by subsystem in the source tree.

Finding members
- Use your editor's search to find symbols. The `tests/` and `examples/` directories demonstrate how many namespace members are intended to be used.

