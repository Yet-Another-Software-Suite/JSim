# Class Member Functions — Guide

What to expect
- Member functions implement behavior for simulation objects: stepping, applying forces, querying state, and configuring properties.

Inspecting implementations
- Find definitions in `core/src` and corresponding headers in `core/include` to understand parameter semantics and lifetime expectations.

