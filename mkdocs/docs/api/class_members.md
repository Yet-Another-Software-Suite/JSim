# Class Members — Guide

Overview
- Class members include data fields, methods, nested types, and inner enums. For simulation objects, members typically expose configuration and current state (position, velocity).

Best practices
- Prefer accessor methods instead of exposing mutable public fields.

