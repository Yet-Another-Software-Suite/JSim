# Typedefs & Type Aliases — Guide

Overview
- Type aliases simplify complex template or vector types. Look for `using` or `typedef` declarations in `core/include` headers.

Recommendation
- Use aliases that improve readability in your code; inspect existing aliases for common patterns.

