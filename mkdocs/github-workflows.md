# CI / GitHub Workflows

This repository provides CI workflows under `.github/workflows/` to build, test, and publish documentation and artifacts.

Common tasks
- Build native code and run C++ tests
- Run Python unit tests
- Generate documentation artifacts (Doxygen, Javadoc, MkDocs site)

Adjust workflow YAMLs in `.github/workflows/` to change CI behavior.
